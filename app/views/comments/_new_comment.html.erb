<div>
  <div>You can comment in the below box: </div>
  <%= form_for comment, url: post_comments_path(comment.post, comment) do |f| %>
    <%= f.text_field :message %>
    <%= f.submit 'Comment' %>
  <% end %>
</div>
